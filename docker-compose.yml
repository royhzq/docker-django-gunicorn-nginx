version: '3'

services:
  djangoapp:
    build: .
    volumes:
      - .:/app
    restart: always

  nginx:
    image: nginx:1.13
    ports:
      - 8000:80
    volumes:
      - ./config/nginx/conf.d:/etc/nginx/conf.d
      - ./static:/static
    restart: always
    depends_on:
      - djangoapp